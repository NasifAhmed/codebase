#include <bits/stdc++.h>

using namespace std;

int main() {
    
    int size = 0;
    cin >> size;

    int array[] = new int[size];

    while() {

        if(i==0) {
           continue; 
        }
        
        if(size[i] < size[i-1]) {
            size[i] = size[i]++;
            turn++;
        } 

    }


}
