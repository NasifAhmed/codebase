#include <bits/stdc++.h>

using namespace std;

int main() {
    int n,thinks,hard;

    cin >> n;

    while(n--) {
        cin >> thinks;
        if(thinks==1) {
            hard = 1;
        } 
    }

    if(hard==1) {
        cout << "HARD\n";
    } else {
        cout << "EASY\n";
    }

     return 0;
}